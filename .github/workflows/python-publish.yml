name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '16 8 * * 2'

jobs:
  analyze:
    name: Analyze ${{ matrix.language }}
    runs-on: ${{ matrix.language == 'swift' && 'macos-latest' || 'ubuntu-latest' }}
    permissions:
      security-events: write
      packages: read  
      actions: read  
      contents: read   
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: python
            build-mode: none

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"

      # Create a directory for artifacts if it doesn't exist
      - name: Create Artifact Directory
        run: mkdir -p artifacts

      # Example command to create a zip file; adjust as needed
      - name: Zip Artifacts
        run: zip -r artifacts/codeql-results.zip path/to/your/results/*  # Change this to your actual results path

      - name: Publish Build Artifacts
        uses: actions/upload-artifact@v2
        with:
          name: codeql-results
          path: artifacts/codeql-results.zip
